[project]
name = "tumi-shared"
version = "0.1.0"
description = "Modelos compartidos de Tumi Analytics (SQLAlchemy)"
requires-python = ">=3.12"
dependencies = ["sqlalchemy>=2.0"]

[tool.setuptools]
packages = ["tumi_shared"]
